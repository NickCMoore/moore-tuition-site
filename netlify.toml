# Netlify config for the Next.js app in /frontend
# Overrides UI settings that incorrectly set publish = base.

[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
